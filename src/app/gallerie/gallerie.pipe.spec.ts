import { GalleriePipe, LikesIconPipe, LikesIconColorPipe } from './gallerie.pipe';

describe('GalleriePipe', () => {
  it('create an instance', () => {
    const pipe = new GalleriePipe();
    expect(pipe).toBeTruthy();
  });
});

